{
  "name": "task-tracker",
  "version": "1.0.0",
  "description": "Basic HTTP server that allows you to manage tasks",
  "license": "ISC",
  "author": "BaileyKH",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
